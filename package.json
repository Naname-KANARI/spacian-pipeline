{
  "name": "spacian-pipeline",
  "version": "1.0.0",
  "description": "SPACiAN news pipeline — collect, score, generate, publish",
  "type": "module",
  "scripts": {
    "collect": "tsx --env-file=.env src/collect.ts",
    "score": "tsx --env-file=.env src/score.ts",
    "generate": "tsx --env-file=.env src/generate.ts",
    "check-pending": "tsx --env-file=.env src/check-pending.ts",
    "publish": "tsx --env-file=.env src/publish.ts",
    "relate": "tsx --env-file=.env src/relate.ts",
    "pipeline": "npm run collect && npm run score && npm run generate && npm run check-pending && npm run publish && npm run relate",
    "reimage": "tsx --env-file=.env src/reimage.ts",
    "reimage-dispatch": "tsx --env-file=.env src/reimage.ts --dispatch",
    "healthcheck-images": "tsx --env-file=.env src/healthcheck-images.ts",
    "cli": "tsx --env-file=.env src/cli.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cheerio": "^1.2.0",
    "nodemailer": "^9.0.1",
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "@types/cheerio": "^0.22.35",
    "@types/node": "^25.9.2",
    "@types/nodemailer": "^8.0.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
